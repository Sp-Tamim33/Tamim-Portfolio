// Type JS
var typed = new Typed('.textType', {
    strings: ['A Web Developer'],
    smartBackspace: true,
    typeSpeed: 100,
    loop: true,
    backSpeed: 100
});

//AOS
AOS.init({
	 offset: 150,
	 duration: 1000
});